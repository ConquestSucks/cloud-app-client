import { model, Model, modelAction, } from "mobx-keystone";

@model("File")
export class File extends Model({
}) {
    @modelAction
    uploadFile() {
        
    }

    @modelAction
    UpdateFile() {
        
    }

    @modelAction
    DeleteFile() {
        
    }    
}
